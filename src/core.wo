let dump: Forall A => (x: A) -> Void ~
  intrinsic "dump"
end

let read: String ~
  intrinsic "read"
end

let (+) x y: I64 -> I64 -> I64 =
  (instrinsic "addI64") x
end

let (==) x y: I64 -> I64 -> I64 =
  (instrinsic "cmpI64") x
end

let (%) x y: I64 -> I64 -> I64 =
  (instrinsic "modI64") x
end

