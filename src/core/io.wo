let dump: (Any a => x: a) -> I8 ~
  __intrinsic("dump")
end

let dump x ~ __intrinsic("dump")

let add: (Num a => x: a -> y: a -> a) =
  x + y
end

let add x y = x + y

let read: String ~ 
  __intrinsic("read")
end

let read ~ __intrinsic("read")
